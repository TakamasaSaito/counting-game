# ひらがな探しアプリ「ひらがなたからさがし」Claude Code 実装指示書(MVP)

## 1. プロジェクト概要

3〜4歳の子どもが「宝探しゲーム」としてひらがなに親しむWebアプリ。
書く練習ではなく「見て・聞いて・探して・タップする」成功体験の積み重ねを最優先とする。

- MVP対象文字: **あ・い・う・え・お** の5文字のみ
- 検証したいこと: 楽しいか / 飽きないか / 繰り返し遊ぶか
- 完成度より継続利用。演出のテンポと気持ちよさを最重視する

## 2. 技術スタック(必須遵守)

- **単一HTMLファイル**(index.html)にHTML/CSS/JSをすべて含める
- Vanilla JS(フレームワーク・ビルドツール・npm不使用)
- 外部アセットなし(画像・音声ファイルを使わない)
  - 効果音は **Web Audio API で合成**する(下記 §8)
  - 問題読み上げは **Web Speech API(speechSynthesis, ja-JP)**
  - キャラクター・星などの絵は絵文字またはインラインSVGで表現
- デプロイ: Netlify(index.html をドラッグ&ドロップできる構成)
- Git運用: **commit はするが push はしない**(push はマイルストーン時に手動でまとめて行う)

## 3. iOS Safari 対応ルール(必須)

- 音声(AudioContext / speechSynthesis)は**ユーザー操作後でないと再生不可**。
  スタート画面の「はじめる」ボタンのタップハンドラ内で
  `AudioContext.resume()` と `speechSynthesis.speak(空発話)` を実行して初期化する
- `alert()` / `confirm()` / `prompt()` は使用禁止。ダイアログはすべて自前実装
- ダブルタップズーム防止: `touch-action: manipulation;` を全ボタンに適用
- 長押しでの文字選択・コールアウト防止: `user-select: none; -webkit-touch-callout: none;`
- viewport: `width=device-width, initial-scale=1, viewport-fit=cover, user-scalable=no`
- 連打対策: 正解判定後は次の問題表示まで全タイルのタップを無効化する

## 4. 画面構成

### 4.1 スタート画面
- アプリタイトルと大きな「はじめる」ボタン(音声初期化を兼ねる)
- 「はじめる」→ 続きがあれば前回のレベルから、初回はレベル1から自動開始
- 画面右下に小さく目立たない「⚙」(親用メニュー、§9)

### 4.2 レベル選択画面
- レベル1〜4のカード。アンロック済みのみタップ可、未開放は🔒表示
- スタート画面から「レベルをえらぶ」小ボタンで遷移可能
- 自動進行(一本道)とレベル選択の**両方**をサポートする

### 4.3 ゲーム画面
- 上部: 問題表示「**あ** をさがそう!」+ スピーカーボタン(タップで再読み上げ)
- 中央: ひらがなタイルのグリッド
- 下部: セット進捗(10問中の何問目か。★や●で子どもにも分かる表現)

### 4.4 クリア画面(10問1セット終了時)
- 紙吹雪+星のアニメーション、「すごい!」音声とファンファーレ
- 「つぎへ」(次レベルへ/同レベル再挑戦)と「おわる」の2ボタン

## 5. レベル設計

| レベル | 表示タイル数 | グリッド例 |
|---|---|---|
| 1 | 4 | 2×2 |
| 2 | 6 | 2×3 / 3×2 |
| 3 | 9 | 3×3 |
| 4 | 12 | 3×4 / 4×3 |

- 出題対象は常に「あいうえお」から1文字をランダム選択(直前の問題と同じ文字は避ける)
- 正解タイルは**必ず1枚だけ**。残りは他の4文字からのランダム(重複可)
- **類似字ルール**: レベル1〜2では「あ」と「お」を同一画面に同時に出さない
  (形が似ており3歳には難しすぎるため)。レベル3以降は許可
- タイル配置は毎問シャッフル

## 6. 進行・正誤判定ルール

- 正解: ピンポーン音 → 正解タイルが弾むアニメ+星が飛ぶ → 「できたね!」音声 → 約2秒後に自動で次の問題へ
- 不正解: 減点なし。「もう一回!」音+押したタイルが軽く揺れる。何度でも押せる
- **3回間違えたら**: 正解タイルがゆっくり光る(パルスアニメーション)+問題文をもう一度読み上げる。ヒント後の正解も通常どおり祝う(失敗体験を作らない)
- 10問で1セット。セットクリアで次レベルをアンロックし、クリア画面へ
- レベル4クリア後は「レベル4をもういちど」を提示(MVPではここが終点)

## 7. 音声読み上げ(Web Speech API)

- `speechSynthesis` + `SpeechSynthesisUtterance`、`lang: 'ja-JP'`
- 単音だと不自然になるため、**必ず文として読ませる**: 「『あ』をさがしてね!」
- rate はやや遅め(0.9 程度)、pitch は少し高め(1.2 程度)で子ども向けの声に寄せる
- 読み上げ前に `speechSynthesis.cancel()` で前の発話を止める(連打・自動遷移対策)
- 音声非対応環境でもゲーム自体は動作すること(読み上げは無音スキップ)

## 8. 効果音(Web Audio API 合成)

外部ファイルを使わず OscillatorNode + GainNode で合成する。

- 正解「ピンポーン」: 2音チャイム(例: E5 → C6、sine波、短いディケイ)
- 不正解: 低めの短い1音(例: A3、柔らかく。怖い音・ブザー音にしない)
- セットクリア: 上昇アルペジオのファンファーレ(3〜5音)
- タップ時の軽いクリック音(任意)
- 音量は控えめに統一し、マスターGainを1つ持たせる

## 9. 進捗保存(localStorage)

- キー: `hiragana-hunt-v1` にJSONで一括保存
- 保存内容: アンロック済み最高レベル、現在のレベル、通算正解数
- **親用リセット**: ⚙ボタンは**3秒長押しで開く**(子どもの誤操作防止)。
  メニュー内に「進捗をリセット」(自前実装の確認ダイアログ付き)と「音量オン/オフ」
- localStorage が使えない環境(プライベートブラウズ等)でも例外を出さず動作すること

## 10. レスポンシブ・UI要件

- **iPhone縦持ち・iPad横持ちの両対応**。CSS Grid + `aspect-ratio` でタイルを正方形に保ち、
  画面サイズと向きに応じてグリッドとフォントサイズを流動的に調整(`vmin` 基準を推奨)
- タップ領域は最小 **88px 相当**以上。タイル間の余白を十分に取り誤タップを防ぐ
- タイルの文字は極太のゴシック体(システムフォントでよい。例: `font-weight: 900`)で画面内最大級の大きさ
- 文字以外の装飾情報は極力排除。1画面1目的
- `prefers-reduced-motion` を尊重(アニメーションを簡略化)

## 11. デザイン方針

- 明るくやさしいパステル基調のカラフルな配色。ただし文字タイルは**白地に濃色文字**で
  可読性を最優先(装飾は背景とごほうび演出に寄せる)
- タイルごとに枠色を変えるなど「宝箱・カード」らしい楽しさを出す
- 正解演出は派手に(星・紙吹雪・キャラクターが喜ぶ)、不正解演出は控えめに
- 応援キャラクターを1体、絵文字またはシンプルなSVGで画面隅に配置し、
  正解時にジャンプ等のリアクションをさせる

## 12. コード構成の指針

単一ファイル内でも以下のモジュール分割を意識すること:

- `GameState`: レベル・問題番号・正解数・間違い回数・localStorage入出力
- `QuestionGenerator`: 出題文字選択・タイル生成(類似字ルール含む)
- `AudioEngine`: 効果音合成と読み上げ(初期化・マスター音量含む)
- `UI`: 画面遷移・描画・アニメーション制御
- 画面遷移は state machine 的に(start / levelSelect / game / clear / parentMenu)

## 13. 受け入れ条件(セルフチェックリスト)

- [ ] iPhone Safari(縦)と iPad Safari(横)の両方でレイアウト崩れなし
- [ ] 「はじめる」タップ後、問題音声と効果音が iOS で確実に鳴る
- [ ] 正解→演出→約2秒→次の問題、が途切れずテンポよく回る
- [ ] 3回誤答で正解タイルが光り、再読み上げされる
- [ ] レベル1〜2の画面に「あ」「お」が同時に出ない
- [ ] 10問クリアで次レベルがアンロックされ、リロード後も保持される
- [ ] ⚙3秒長押し→リセットで初期状態に戻る
- [ ] 連打・画面回転・音声OFF環境で例外が発生しない

## 14. 将来拡張(MVPでは実装しない。設計だけ阻害しないこと)

か行以降の50音追加 / 絵と文字の結びつけゲーム / 名前モード / 毎日チャレンジ・バッジ /
保護者向けレポート / 音声キャラクター変更 / 難易度自動調整。
文字データは配列で一元管理し、行の追加が容易な構造にしておく。
