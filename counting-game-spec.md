# 実装指示書: 数かぞえアプリ「いくつあるかな」(仮称)

Claude Code向け実装指示書。前作「ひらがなたからさがし」(TakamasaSaito/hiragana-hunt)の
index.html をコピーして改造する方式で作る。新規リポジトリ: TakamasaSaito/counting-game

---

## 1. 確定要件(要件定義済み・変更不可)

| 項目 | 決定内容 |
|---|---|
| 対象 | 3〜4歳児 |
| 数の範囲 | 1〜10(レベルなし、毎問ランダム) |
| 答え方 | 3択ボタン、表記は数字のみ(「3」) |
| タップ数え | 任意。モノをタップすると「いち!」等の音声+演出。数えずに直接回答も可 |
| モノの種類 | 果物・動物・乗り物を混ぜて多様に(絵文字ベース) |
| セット | 10問1セット+クリア演出(紙吹雪+ファンファーレ) |
| 進捗保存 | localStorage累計記録+親メニューで確認(前作同様) |
| 端末 | iPhone縦が主、iPad横も対応 |
| 問題文音声 | 「いくつあるかな」に統一(助数詞問題の回避。「なんこ」は動物で不自然) |

## 2. ゲームフロー

1. スプラッシュ(1.5秒、タップスキップ可)→ スタート画面「はじめる」
   - 「はじめる」タップで AudioContext 初期化(iOS必須)
2. 問題画面:
   - 画面中央エリアに絵文字が N 個(N=1〜10)散らばって表示(同一種類、例: 🐶×4)
   - 音声「いくつあるかな」を自動再生
   - 子どもは任意で絵文字をタップ → タップ順に「いち」「に」…音声+弾むアニメ、
     タップ済みは薄く(opacity 0.4程度)+小さくカウント番号バッジ表示
   - 同じ絵文字の再タップは無効(カウント重複防止)
   - 画面下部に3択の数字ボタン。いつでも回答可能
3. 正解: ピンポーン(2音チャイム)+星+キツネ🦊リアクション+「できたね!」等の
   応援音声 → 約2秒で次問題へ(この間、全タップ無効化)
4. 不正解: 減点なし、柔らかい1音(ブザー禁止)、何度でも回答可。
   3回誤答で正解ボタンが光る+問題文再読み上げ
5. 10問クリア → クリア画面(紙吹雪+上昇アルペジオ)→ スタート画面へ

## 3. 出題ロジック(QuestionGenerator)

- 正解数 N: 1〜10からランダム。ただし1セット内で同じNの連続出題は避ける
- 絵文字プール(1問につき1種類をランダム選択):
  - 果物・食べ物: 🍎🍌🍓🍊🍇🍑🍰🍩
  - 動物: 🐶🐱🐰🐼🐸🐥🐟🦋
  - 乗り物: 🚗🚌🚒🚑🚀⛵🚲🚁
  - ※同一セット内でカテゴリが偏らないよう軽くシャッフル
- 3択の誤答生成:
  - 正解±1〜±2を優先して2つ生成(1〜10の範囲内、重複なし)
  - 例: 正解3 → 選択肢 {2,3,4} や {3,4,5} 等。ボタン順はシャッフル
- 絵文字配置:
  - コンテナ内にランダム配置、ただし**重なり禁止**(タップ領域確保のため
    最低88px相当の間隔をグリッド+ジッター方式などで保証)
  - N=10でもiPhone縦画面に収まること(絵文字サイズはNに応じて可変、vmin基準)

## 4. 技術スタック・コード構成(前作と同一)

- 単一 index.html + Vanilla JS + audio/ 静的アセット。フレームワーク・ビルドなし
- モジュール分割: GameState / QuestionGenerator / AudioEngine / UI
- 画面遷移 state machine: splash / start / game / clear / parentMenu
  (※前作の levelSelect は今回不要。レベルなしのため)
- localStorage: JSON一括保存、try-catch例外ガード必須、親メニューにリセットボタン
  - 保存内容: 累計正解数、セットクリア回数、数字別正答統計(1〜10各数の正答/出題数)
- ファイル/フォルダ名は英数字のみ

## 5. iOS Safari 対応(必須・前作実証済みルール)

- [ ] 音声は「はじめる」タップで初期化(AudioContext.resume + 無音再生アンロック)
- [ ] alert/confirm/prompt 禁止。ダイアログ自前実装
- [ ] touch-action: manipulation / user-select: none / -webkit-touch-callout: none
- [ ] viewport: width=device-width, initial-scale=1, viewport-fit=cover, user-scalable=no
- [ ] 正解判定後〜次問題まで全タップ無効化(連打対策)
- [ ] iPhone縦・iPad横両対応(vmin基準、CSS Grid、aspect-ratio)
- [ ] タップ領域88px以上、prefers-reduced-motion 尊重
- [ ] apple-touch-icon 180px + icon 512px、apple-mobile-web-app-capable

## 6. 音声(VOICEVOX: ずんだもん あまあま)

### 生成ファイル一覧(scripts/generate_audio.py で一括生成)

| ファイル | 文言 | 備考 |
|---|---|---|
| count_1.wav〜count_10.wav | 「いち」〜「じゅう」 | タップ数え用。テンポよく短く |
| question.wav | 「いくつ あるかな」 | 問題文。全問共通 |
| praise_1.wav〜praise_3.wav | 「できたね!」「すごい!」「やったね!」 | 正解時ランダム |
| hint.wav | 「もういちど かぞえてみよう」 | 3回誤答時 |
| clear.wav | 「ぜんぶ できたね!」 | セットクリア時 |

### 生成時の品質ルール(前作の結論・厳守)

- 「?」は使わない。is_interrogative を全 accent_phrases で false に
- prePhonemeLength=0.3、冒頭ポーズ0.70秒(頭切れ対策)
- 新規文言はまず audio/test/ に複数パターン生成 → 本人が聞き比べ → 採用後に一括生成
- WSLからVOICEVOX接続: `ip route show | grep default` のIP:50021、まず /version で疎通確認
  (Windows側 netsh portproxy 設定済み。VOICEVOXアプリをWindows側で起動しておく)
- 再生: 起動時に全wavプリロードしMap管理。失敗時のみ speechSynthesis フォールバック
  (play()のAbortErrorはフォールバック扱いにしない)
- クレジット「音声: VOICEVOX / ずんだもん」を親メニュー内に必須表示

### タップ数え音声の注意

- 連続タップ時は前の再生を中断して次を再生(重ね再生しない)。
  Audio要素のcloneではなく currentTime=0 リセット方式か、数え上げ専用チャンネルを1本用意

## 7. 効果音(Web Audio API合成・外部ファイル不要)

- 正解: E5→C6 の2音チャイム
- 不正解: 低め柔らかい1音(ブザー禁止)
- クリア: 上昇アルペジオ
- タップ数え時: 音声と同時に軽いポップ音(短いsine、任意)
- マスターGain 1つで音量統一

## 8. UI・演出(前作の確立パターンを踏襲)

- パステル基調カラフル配色、紫系グラデ背景、白タイル+濃色文字、カラフル枠線
- 応援キャラ: キツネ🦊(正解時リアクション)
- ホームボタン🏠(左上・控えめ)でスタート画面へ
- 親メニュー: ⚙を3秒長押し(Pointer Events、contextmenu抑止)
  - 表示内容: 累計正解数、クリア回数、数字別正答率、進捗リング、リセット、VOICEVOXクレジット
- 3択ボタン: 大きく(88px以上)、数字のみ表記、押下フィードバックあり

## 9. 受け入れ条件チェックリスト

### 機能
- [ ] 1〜10の問題がランダム出題され、3択で回答できる
- [ ] 絵文字タップで「いち」〜「じゅう」音声+演出、タップ済みは薄くなり再タップ無効
- [ ] 数えずに直接3択回答しても正常動作する
- [ ] 3回誤答で正解ボタンが光り、問題文が再読み上げされる
- [ ] 10問でクリア画面(紙吹雪+ファンファーレ)が出る
- [ ] 進捗がlocalStorageに保存され、親メニューで確認・リセットできる
- [ ] localStorage無効環境でもクラッシュしない(例外ガード)

### 表示
- [ ] iPhone縦: N=10でも絵文字が重ならず全て見える・タップできる
- [ ] iPad横: レイアウト崩れなし
- [ ] タップ領域はすべて88px以上

### 音声
- [ ] iPhone実機Safariで「はじめる」タップ後、全音声が再生される
- [ ] 連続タップ数えで音声が破綻しない
- [ ] wav読み込み失敗時にspeechSynthesisフォールバックが動く

### 規律
- [ ] Claude Code は commit のみ、push しない(push は本人がマイルストーン時)
- [ ] 子どもの名前等の個人情報をコード・コミットに含めない
- [ ] ファイル名は英数字のみ

## 10. 開発手順

1. 前作リポジトリの index.html / scripts/ / icons/ をコピーして新規作業ディレクトリへ
2. `gh repo create TakamasaSaito/counting-game --public --source=. --remote=origin`
   (最初のコミット後に `git branch -M main`、push は本人が実行)
3. index.html 改造: ひらがな出題ロジック → 数かぞえロジックへ差し替え
4. 音声テスト生成(audio/test/)→ 本人聞き比べ → 本生成
5. `python3 -m http.server 8080` → Windowsブラウザで動作確認
6. 本人が push → Netlify連携(Build command空欄、Publish=/)→ iPhone実機確認

## 11. アプリ名について(要決定・実装ブロッカーではない)

引き継ぎ書の仮称「これはなんこかな?」は助数詞の都合で再考推奨。
問題文音声「いくつあるかな」に合わせた候補:
- 「いくつあるかな」(そのまま)
- 「かずのたからさがし」(前作とのシリーズ感)
- 「かぞえてあそぼ」

仮タイトルは「いくつあるかな」で実装を進め、確定後にtitleタグ・スプラッシュのみ差し替え。
